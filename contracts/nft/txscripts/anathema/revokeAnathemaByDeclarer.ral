TxScript RevokeAnathemaByDeclarer(
    scroundrelAddress: Address,
    revokerAddress: Address,
    fealtyId: ByteVec
) {
    Fealty(fealtyId).revokeAnathemaByDeclarer(revokerAddress, scroundrelAddress)
}
