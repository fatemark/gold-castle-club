TxScript RevokeAnathemaByHighLord(
    revokerAddress: Address,
    scroundrelAddress: Address,
    fealtyId: ByteVec
) {
    Fealty(fealtyId).revokeAnathemaByHighLord(revokerAddress, scroundrelAddress)
}
