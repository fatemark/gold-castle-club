TxScript ChangeOwner(
    nftId: ByteVec,
    nftCollectionId: ByteVec
) {
    NFTPublicSaleCollectionRandomWithRoyalty(nftCollectionId).changeOwner{callerAddress!() -> nftId: 1, ALPH: dustAmount!()}(nftId)
}
